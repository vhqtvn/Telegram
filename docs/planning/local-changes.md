# Local changes registry (fork patch map)
Purpose: durable map of every local commit on top of upstream DrKLO/Telegram, so upstream release bumps are mechanical. Maintained at every release bump and whenever a local commit lands. Backlog (docs/planning/backlog.md) tracks task status; this file tracks the PATCH SURFACE.

## Upstream tracking
- Upstream base this fork sits on: `62b56a07c` "update to 12.10.1 (7038)" (2026-08-25).
- Upstream has NO git tags; each release is ONE squashed "update to X.Y.Z (build)" commit; cadence ~2.6/month. Track releases by these commit hashes (`git log upstream/master --grep="update to" --oneline`).
- Remote name for upstream: `upstream` → https://github.com/DrKLO/Telegram.git (to be added by operator).
- Sync check 2026-09-15: `git fetch upstream` succeeded (3.4s); `upstream/master` == base `62b56a07c` — 12.10.1 (7038) is the newest upstream release, zero drift, iteration-1 lands conflict-free. Non-fatal fetch warning: submodule `TMessagesProj/jni/libtgvoip` commit `2cf2a45ac` unreachable from our submodule remote (informational only).
- Note: upstream tags exist historically (5.13.0 → 11.4.2, fetched with the remote) but upstream stopped tagging after 11.4.2 — "update to" commit hashes remain the release tracking mechanism.

## Fork product identity
- applicationId: `vn.vhn.telegram` (debug `.beta`, standalone `.web`) via `APP_PACKAGE` in `gradle.properties`; java package/namespace stays `org.telegram.messenger` (no code move).
- api_id/api_hash: put `TELEGRAM_API_ID` / `TELEGRAM_API_HASH` in `local.properties` (gitignored); `:TMessagesProj:patchBuildVars` injects them into BuildVars.java at build time and restores the committed placeholders when the build ends. The committed `APP_ID = 4` / `APP_HASH` placeholders stay in the tree.
- keystore/signing: signing values resolve in three tiers — gradle properties first (non-dummy), then the optional gitignored `.local/config/android-keys/signing.properties`, then committed dummies (`android`/`androidkey` + `TMessagesProj/config/release.keystore`). signing.properties keys: `RELEASE_STORE_FILE` (root-relative or absolute), `RELEASE_STORE_PASSWORD`, `RELEASE_KEY_ALIAS`, `RELEASE_KEY_PASSWORD`, plus `RELEASE_STORE_PASSWORD_FILE` / `RELEASE_KEY_PASSWORD_FILE` (file whose trimmed content is the password — operator layout: `.local/config/android-keys/.password`). Operator keystore: `.local/config/android-keys/release.keystore`, alias `telegram-fold`; both `RELEASE_STORE_PASSWORD_FILE` and `RELEASE_KEY_PASSWORD_FILE` point at the same `.password` file (operator-chosen single-password layout — the green signed release build confirms the layout works; not a claim about keystore internals). Real keystores/passwords stay under gitignored `.local/` — NEVER committed.
- product name: `VHGram` / `VHGram Beta` (`AppName`/`AppNameBeta` in `TMessagesProj/src/main/res/values/strings.xml`). Launcher icon is still upstream's Telegram icon — MUST be replaced before any store distribution (Telegram ToS §2.4 forbids official-logo reuse). Localized `AppName` overrides in `values-*/strings.xml` (ru/de/es/it/ko/nl/ar/pt-rBR/uk, ~10 locales) still say "Telegram" and shadow the label on those device locales — follow-up surface.
- registration: a `vn.vhn.telegram` api_id/api_hash pair must be obtained from https://my.telegram.org and used with the same account-type (`vn.vhn.telegram`, auth.xml + ContactsController).

## Maintenance rules (binding)
- R1: Inline surgical patches only for state/correctness fixes in historically stable regions (verify stability via `git log -L` before choosing inline).
- R2: All GROWING policy logic (breakpoints, pane policy, posture hooks) lives in a fork-owned helper class (planned: `org.telegram.messenger.FoldableConfig`, static, dependency-light, Java, Telegram idiom — introduced at FOLD-002, NOT retrofitted onto iteration 1). Upstream files get one-line call sites; fork-owned file paths cannot conflict with upstream by construction.
- R3: Rebase-per-release: rebase the local themed commit series onto each new upstream "update to" commit; NEVER squash local commits into the upstream base; keep commits themed with `foldables:` prefix (other themes allowed as they appear); one conceptual change per commit.
- R4: This registry is updated at every release bump (new base hash) and at every local commit landing.
- R5: Post-bump verification gate: `./gradlew :TMessagesProj:compileDebugJavaWithJavac` then `./gradlew :TMessagesProj_App:assembleAfatDebug` (env: JAVA_HOME=~jdk/jdk-21*, PATH prepend ~/android-sdk/cmake/3.22.1/bin); fold-matrix smoke when hardware available; `apkdiff.py` is valid ONLY for own-vs-own regression diffing (fork-vs-official byte comparison is impossible once any local change lands — dex differs).

## Release-bump runbook
1. `git fetch upstream`
2. Find target: `git log upstream/master --grep="update to" --oneline -5`
3. `git rebase <new-release-hash>` (resolve conflicts per local commit, guided by the entries below)
4. Run verification gate (R5)
5. Update this file: new base hash + any notes from conflict resolution
6. Operator pushes: `git push origin HEAD --force-with-lease` (rebase rewrites SHAs)

## Registry entries
| Commit | Theme | Files | Why | Conflict-prone regions / notes |
|---|---|---|---|---|
| `724fda54e` | harness (not app code) | `.opencode/`, `.vh-agent-harness/`, `docs/coordination/`, `docs/planning/` seeding, `opencode.jsonc`, `Makefile`, AGENTS/CLAUDE | Repo-resident agent harness | No upstream-code overlap → no conflict surface |
| `24b04a16e` | foldables iter-1 | `TMessagesProj/src/main/java/org/telegram/messenger/AndroidUtilities.java` (+18/−9), `TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java` (+13/−9), `TMessagesProj/src/main/java/org/telegram/ui/LaunchActivity.java` (+25/−0) | Fix state/sizing across runtime size-class flips: D1 roundMessageSize recompute in `resetTabletFlag()`; D2 pause live-view fragments in `invalidateTabletMode()` flip branch (draft persistence); D3 `captureScrollPositionForRecreate()` scroll restore; D4 wasTablet debug alignment | Hunks sit in STABLE sub-regions: `resetTabletFlag` 1 upstream touch ever; roundMessageSize formula unchanged since 2017; theme-preview block ~stable (1 touch 2026); `invalidateTabletMode` MODERATE (2 edits 2026 — watch at bumps). Whole files churn every release (AndroidUtilities ~51%/LaunchActivity ~65%/ChatActivity ~80% of releases) so expect textual context drift, not logic conflicts. |
| (with the above) | docs | `docs/planning/backlog.md` (+2 rows FOLD-001/FOLD-002) | Task status ledger | No upstream overlap; commit SEPARATELY from code per split-commit rule |
| `45dc6340c` | rebrand iter-2 | `gradle.properties` (APP_PACKAGE), `TMessagesProj/google-services.json` + `TMessagesProj_App/google-services.json` (+1 duplicate client each for `.beta`/`.web`/base), `TMessagesProj/src/main/res/xml/auth.xml`, `sync_contacts.xml`, `auth_menu.xml` (accountType/targetPackage), `ContactsController.java` (5 account-type literals, ±10), `BuildVars.java` (IS_BILLING_UNAVAILABLE=true, SUPPORTS_PASSKEYS=false) | applicationId `vn.vhn.telegram` per fork identity (see section above) | **`gradle.properties` APP_PACKAGE line is a RECURRING conflict at EVERY upstream bump** (upstream rewrites APP_VERSION_* in the same block): resolution = keep our `APP_PACKAGE=vn.vhn.telegram`, take upstream's `APP_VERSION_CODE`/`APP_VERSION_NAME`. ContactsController hunks are account-type sync with `auth.xml`/`sync_contacts.xml` — if upstream renames the literal again, rename all together. google-services.json: upstream touches it rarely; on conflict keep BOTH upstream clients and our `vn.vhn.*` duplicates. BuildVars two booleans: upstream comments mandate fork values; re-assert ours if upstream flips them. |
| `8c40cc5d0` | credentials iter-2 | `TMessagesProj/build.gradle` (+~60: patchBuildVars/restoreBuildVars + buildFinished restore), `TMessagesProj_App/build.gradle` (±7: RELEASE_STORE_FILE storeFile override) | Real api credentials via local.properties, keystore path via gradle property; placeholders/dummies stay committed | Build tails of both files — low-churn regions (TMessagesProj tail = getProps + gms plugin; App signing block stable). Upstream rewrites these files wholesale occasionally: re-apply mechanically, mechanism is self-contained. BuildVars APP_ID/APP_HASH lines are now BUILD-MANAGED — never hand-edit or commit real values there. |
| `003b58bd6` | signing iter-3 | `TMessagesProj_App/build.gradle` (+28/−8: `signingValue` resolver + optional `.local/config/android-keys/signing.properties`, `*_PASSWORD_FILE` indirect passwords) | Real keystore + passwords from gitignored signing.properties (operator `.password` layout); gradle props still take precedence, committed dummies stay last | Same signing-block tail as `8c40cc5d0` (stable region); on upstream wholesale rewrites re-apply mechanically. Dummy fallback is the root-relative equivalent of the old `../TMessagesProj` path — no behavior change when `.local/` is absent. |
| `ab2b39f2a` | rebrand iter-3 | `TMessagesProj/src/main/res/values/strings.xml` (2 lines: AppName/AppNameBeta) | Product label `VHGram` / `VHGram Beta` | Lines 3-4 of a huge file; upstream rarely touches the AppName defaults. values-*/ localized AppName overrides still say "Telegram" (~10 locales) — follow-up if a full-locale rebrand is wanted. |

## Known upcoming surface (not yet landed)
- 16 KB page alignment: NO local commit needed — upstream `aa5fe2ee8` ("update to 12.0.0 (6163)") already ships `-Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384` on the only from-source shared target (`TMessagesProj/jni/CMakeLists.txt` `target_link_options(tmessages.49)`; `Application.mk` carries the ndk-build form, unused by this CMake build). Verified 2026-09-15 on the afat release APK: `libtmessages.49.so` LOAD alignment 0x4000 on all 4 ABIs; `zipalign -c -P 16 4` passes. Prebuilt `liblanguage_id_l2c_jni.so` (mlkit language-id AAR) is 0x1000 on armeabi-v7a/x86 — no 16 KB-page devices exist for those ABIs (requirement is arm64-only, where it is 0x4000); revisit only if mlkit ships an update or Play flags it.
- FOLD-002 (portrait medium-width pane policy) will target `LaunchActivity.setupActionBarLayout()` (gates at ~L943/L966) + `checkLayout()` (~L1320) + `AndroidUtilities.isSmallTablet()/getMinTabletSide()/getTabletLeftFragmentSize()`. `setupActionBarLayout` is CHURNY (3 upstream edits in 12 months) → this is what triggers R2 (central helper), keeping upstream-side edits to one-line call sites.

Registry created 2026-09-15 by the foldables coordination session; see .opencode/state/workstreams/foldable-ux/ for the full maintenance brief.
